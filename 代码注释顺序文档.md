# MachOView 代码注释顺序文档

本文档用于**分批为项目添加中文注释**时的顺序与批次划分。按“依赖自底向上、同层先头文件后实现”的原则排列，便于你逐批让 AI 协助注释，且每批注释时前置依赖已具备注释，上下文更清晰。

---

## 一、总体原则

1. **先底层、后上层**：被依赖的模块先注释（如 Common、DataController、Layout），再注释依赖它们的模块（如 MachOLayout、Document）。
2. **先 .h、后 .mm**：同一模块先注释头文件（接口与数据结构），再注释实现文件。
3. **Category 随主类**：LoadCommands、LinkEdit、DyldInfo、Exceptions、SectionContents、ObjC、CRTFootPrints 均为 MachOLayout 的 Category，与 MachOLayout 放在同一批或相邻批。
4. **capstone/**：第三方库，**不纳入**本项目的分批注释顺序；仅在引用处（如 SectionContents.mm）做简要说明即可。

---

## 二、依赖层次简表（仅项目内）

| 层次 | 说明 | 文件 |
|------|------|------|
| L0 | 无项目内头文件依赖 | Common.h, Document.h, DataSources.h, PreferenceController.h, AppController.h, Attach.h |
| L1 | 仅依赖系统/框架 | DataController.h |
| L2 | 依赖 DataController / Layout 声明 | ReadWrite.h, Layout.h |
| L3 | 依赖 Layout | MachOLayout.h, FatLayout.h, ArchiveLayout.h |
| L4 | 依赖 MachOLayout（Category） | LoadCommands.h, LinkEdit.h, DyldInfo.h, Exceptions.h, SectionContents.h, ObjC.h, CRTFootPrints.h |

实现文件(.mm) 的依赖与对应 .h 一致，且会跨模块引用（如各 .mm 引用 Common、DataController、ReadWrite 等），故按“模块”分批即可。

---

## 三、推荐分批顺序

以下每批内的文件，建议**按表中顺序**注释（同一批内先 .h 后 .mm）。

### 第 1 批：公共与入口（无项目内依赖）

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | Common.h | 23 | 全局宏、extern 变量、NSSTRING/CSTRING 等 |
| 2 | main.mm | 19 | 入口、pipeCondition/numIOThread 初始化 |
| 3 | Prefix.pch | 9 | 预编译头（若需注释可写简要说明） |

**建议注释重点**：Common.h 中每个宏和 extern 的用途；main 中两变量的作用及与多线程/管道的关系。

1. Common.h
文件头：说明本头为“公共头文件”，并概括用途（全局开关、共享变量、通用宏）。
编译开关：为 MV_NO_MULTITHREAD、MV_NO_ARCHIVER、MV_STATISTICS 各加一行注释，说明打开后的效果。
全局变量：注释 pipeCondition（管道/IO 同步）、numIOThread（当前做 IO 的线程数）、nrow_total / nrow_loaded（行数统计）。
宏：注释 NSSTRING/CSTRING（C 串与 NSString 互转）、N_ELEMENTS/FIRST_ELEM/LAST_ELEM（数组元素个数与首/末元素地址）。

2. main.mm
文件头：说明本文件是应用入口，负责初始化全局 IO 同步变量并启动 Cocoa 主循环。
全局变量：与 Common.h 中声明对应，注释 pipeCondition 和 numIOThread 的用途及与“管道/标准 IO 互斥”的关系。
main 函数：对 pipeCondition 的 alloc/init、numIOThread = 0、以及 NSApplicationMain 的调用各加一行注释。

3. Prefix.pch
文件头：说明这是预编译头、被 Xcode 自动包含，以及用来统一引入 Cocoa 和原子操作、加快编译。
#ifdef _OBJC_：注释为何只在 ObjC 下 #import <Cocoa/Cocoa.h>。
#include <libkern/OSAtomic.h>：说明用于原子操作，并提到可能用于 nrow_loaded/nrow_total 等跨线程统计。

---

### 第 2 批：数据模型核心（被全局依赖）

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | DataController.h | 209 | MVNode、MVTable、MVRow、MVColumns、MVArchiver、MVNodeSaver、通知名、列常量等 |
| 2 | DataController.mm | 1396 | 创建布局、更新树/表、Archiver、createLayouts 分支（Fat/Archive/MachO）等 |

**建议注释重点**：树形结构（MVNode/MVTable/MVRow）如何对应 UI 的左右视图；fileData/realData 区别；createLayouts 如何根据文件类型选择 FatLayout/ArchiveLayout/MachOLayout。

---

### 第 3 批：读写与布局基类

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | ReadWrite.h | 56 | DataController 的读写 Category 声明 |
| 2 | ReadWrite.mm | 368 | read_* / write_* 实现，字节序与 NSRange 推进 |
| 3 | Layout.h | 48 | MVLayout 基类、imageAt:、createDataNode、doMainTasks/doBackgroundTasks 等 |
| 4 | Layout.mm | 123 | MVLayout 基类实现 |

**建议注释重点**：ReadWrite 的 lastReadHex 用途；Layout 的 imageOffset/imageSize 与“当前镜像”的关系；后台线程与 doBackgroundTasks 的协作。

---

### 第 4 批：Mach-O 核心布局（头与数据结构）

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | MachOLayout.h | 89 | CommandVector、Segment/Section/NList 等容器、getSectionByIndex、getSymbolByIndex 等 API |
| 2 | MachOLayout.mm | 2558 | Mach-O 头、Load Command 遍历、Segment/Section 填充、各节点创建入口（可先注释主流程与关键方法） |

**建议注释重点**：各 C++ 容器与 Mach-O 结构的对应关系；is64bit、imageOffset 的使用；与 LoadCommands/LinkEdit 等 Category 的调用关系。

---

### 第 5 批：Load Commands 与 LinkEdit

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | LoadCommands.h | 22 | MachOLayout (LoadCommands) Category 声明 |
| 2 | LoadCommands.mm | 2581 | 各类 LC_* 解析（LC_SEGMENT、LC_SYMTAB、LC_DYSYMTAB、LC_LOAD_DYLIB 等） |
| 3 | LinkEdit.h | 94 | 重定位、符号表、间接符号等声明 |
| 4 | LinkEdit.mm | 1864 | 重定位、符号表、间接符号、stabs 等实现 |

**建议注释重点**：Load Command 与 Mach-O 规范的对应；符号表与 strtab 的配合；32/64 位差异处理。

---

### 第 6 批：Dyld 信息与异常

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | DyldInfo.h | 46 | DyldHelper、Rebase/Bind/LazyBind/Export 等声明 |
| 2 | DyldInfo.mm | 924 | Rebase、Weak Bind、Lazy Bind、Export 等 opcode 解析 |
| 3 | Exceptions.h | 31 | CFI、LSDA、Unwind 相关声明 |
| 4 | Exceptions.mm | 792 | 异常表、LSDA、Unwind 信息解析 |

**建议注释重点**：dyld opcode 语义；LSDA 与异常处理的对应；eh_frame 与 Unwind 的关系。

---

### 第 7 批：Section 内容与 ObjC、CRT

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | SectionContents.h | 73 | 指针、C 字符串、字面量、__text 等 Section 解析声明 |
| 2 | SectionContents.mm | 655 | __text 反汇编（Capstone）、__data/__cfstring 等展示 |
| 3 | ObjC.h | 77 | ObjC 元数据（类/方法/协议/分类等）声明 |
| 4 | ObjC.mm | 3328 | ObjC 类、方法列表、协议、分类、CFString 等解析 |
| 5 | CRTFootPrints.h | 29 | CRT 模式匹配、运行时版本声明 |
| 6 | CRTFootPrints.mm | 1516 | 汇编模式匹配、determineRuntimeVersion 等 |

**建议注释重点**：SectionContents 中 Capstone 的调用约定与错误处理；ObjC 元数据结构与 ABI；CRT 模式与版本检测逻辑。

---

### 第 8 批：Fat 与 Archive

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | FatLayout.h | 16 | Fat 布局接口 |
| 2 | FatLayout.mm | 188 | Fat header、多架构切片、为每切片创建 MachOLayout |
| 3 | ArchiveLayout.h | 31 | Archive 布局、MVObjectInfo |
| 4 | ArchiveLayout.mm | 330 | ar 解析、member 遍历、为每个 object 创建 MachOLayout |

**建议注释重点**：Fat 的 slice 与 MachOLayout 的对应；Archive 的 member 与符号表（若有）的关系。

---

### 第 9 批：文档与数据源（UI 数据绑定）

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | Document.h | 63 | MVDocument、leftView/rightView、MVRightFormatter、IBAction 等 |
| 2 | Document.mm | 853 | 文档加载、布局创建、后台解析、搜索/停止、RVA 切换等 |
| 3 | DataSources.h | 17 | MVDataSourceTree、MVDataSourceDetails 声明 |
| 4 | DataSources.mm | 313 | 左侧树、右侧表的 DataSource/Delegate 实现 |

**建议注释重点**：Document 如何协调 DataController 与左右视图；后台线程与 progressIndicator/stopButton 的配合；树节点与 selectedNode 的联动。

---

### 第 10 批：应用级与附加进程

| 序号 | 文件 | 行数约 | 说明 |
|------|------|--------|------|
| 1 | AppController.h | 31 | 应用委托、菜单等声明 |
| 2 | AppController.mm | 525 | 打开文件、附加到进程、偏好、关于等 |
| 3 | PreferenceController.h | 15 | 偏好窗口声明 |
| 4 | PreferenceController.mm | 25 | 偏好实现（若内容少可合并到一批） |
| 5 | Attach.h | 31 | 附加进程相关类型与 API |
| 6 | Attach.mm | 370 | task_for_pid、读取远程内存、Mach-O 头解析与展示 |

**建议注释重点**：AppController 如何打开文档与弹出 Attach 窗口；Attach 的权限与安全注意点；Preference 的键与 UI 绑定。

---

## 四、使用方式建议

- **每次让 AI 注释时**，可直接引用本顺序，例如：“请按《代码注释顺序文档》第 N 批，为以下文件添加中文注释：……”并粘贴本批文件路径或内容。
- **大文件**（如 MachOLayout.mm、LoadCommands.mm、ObjC.mm）可再拆成“先注释某几个方法/区段”，多轮完成。
- **注释风格**：建议统一为“文件头简要说明 + 类/方法/关键变量行内或块注释”，必要时可注明与 Mach-O 规范或系统 API 的对应关系。

---

## 五、批次与文件一览（速查）

| 批号 | 文件列表 |
|------|----------|
| 1 | Common.h, main.mm, Prefix.pch |
| 2 | DataController.h, DataController.mm |
| 3 | ReadWrite.h, ReadWrite.mm, Layout.h, Layout.mm |
| 4 | MachOLayout.h, MachOLayout.mm |
| 5 | LoadCommands.h, LoadCommands.mm, LinkEdit.h, LinkEdit.mm |
| 6 | DyldInfo.h, DyldInfo.mm, Exceptions.h, Exceptions.mm |
| 7 | SectionContents.h, SectionContents.mm, ObjC.h, ObjC.mm, CRTFootPrints.h, CRTFootPrints.mm |
| 8 | FatLayout.h, FatLayout.mm, ArchiveLayout.h, ArchiveLayout.mm |
| 9 | Document.h, Document.mm, DataSources.h, DataSources.mm |
| 10 | AppController.h, AppController.mm, PreferenceController.h, PreferenceController.mm, Attach.h, Attach.mm |

**说明**：mach-o/ 下的 thread_status.h、thread_status_arm.h 等若需注释，可放在第 5 批（LoadCommands 使用）之前或同批前部，按你偏好即可。
